# Environment variables for Curious Traveler
# This file shows the environment variables used by the application
# Most secrets are stored in Azure Key Vault for production deployments

# =============================================================================
# Azure Developer CLI (azd) Configuration
# =============================================================================
# These are set automatically by 'azd up' or can be configured manually

# Azure environment name (used by azd)
AZURE_ENV_NAME=dev

# Azure subscription ID
AZURE_SUBSCRIPTION_ID=your-subscription-id

# Azure location for resource deployment
AZURE_LOCATION=eastus2

# Azure resource group (auto-generated by azd unless specified)
AZURE_RESOURCE_GROUP=rg-curious-traveler-dev

# =============================================================================
# Azure OpenAI Configuration
# =============================================================================
# These are configured in the Azure App Service Settings

# Azure OpenAI service endpoint
AZURE_OPENAI_ENDPOINT=https://your-openai-service.openai.azure.com/

# GPT-5 Chat deployment name (current model used)
AZURE_OPENAI_GPT5_CHAT_DEPLOYMENT_NAME=gpt-5-chat

# API version for Azure OpenAI
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# =============================================================================
# Azure Speech Services Configuration
# =============================================================================
# These are configured in the Azure App Service Settings

# Speech service endpoint
AZURE_SPEECH_ENDPOINT=https://your-speech-service.cognitiveservices.azure.com/

# Speech service region
AZURE_SPEECH_REGION=eastus2

# =============================================================================
# Azure Key Vault Configuration
# =============================================================================
# Key Vault is used to store sensitive configuration like API keys

# Key Vault endpoint
AZURE_KEY_VAULT_ENDPOINT=https://your-keyvault.vault.azure.net/

# =============================================================================
# Secrets Stored in Key Vault (DO NOT put actual values here)
# =============================================================================
# These secrets are retrieved at runtime from Azure Key Vault:
# - bing-maps-api-key: Bing Maps API key for route calculations
# - azure-openai-api-key: Azure OpenAI API key (if not using managed identity)
# - speech-service-key: Azure Speech service key (if not using managed identity)

# =============================================================================
# Local Development Configuration
# =============================================================================
# For local development, you may need these in local.settings.json instead

# Azure managed identity client ID (for local development authentication)
# AZURE_CLIENT_ID=your-managed-identity-client-id

# =============================================================================
# Mobile App Configuration
# =============================================================================
# These would be used by the Flutter mobile app

# Azure Function App URL (after deployment)
# AZURE_FUNCTION_APP_URL=https://app-curious-traveler-dev.azurewebsites.net

# Function app authentication key (if using function-level auth)
# AZURE_FUNCTION_KEY=your-function-key

# =============================================================================
# Notes for Developers
# =============================================================================
# 1. For production: Use Azure App Settings and Key Vault for secrets
# 2. For local development: Use local.settings.json in the Functions project
# 3. Never commit actual secret values to source control
# 4. Use 'azd up' to provision and configure Azure resources automatically
# 5. Managed Identity is preferred over API keys for Azure service authentication