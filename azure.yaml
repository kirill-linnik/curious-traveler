# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: curious-traveler
metadata:
  template: curious-traveler@0.0.1-beta

hooks:
  postdeploy:
    shell: pwsh
    run: hooks/postdeploy.ps1

services:
  api:
    project: ./src/api
    language: dotnet
    host: appservice